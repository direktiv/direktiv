workflow test

get("namespace", "namespace-test-request-original")
get("namespace", "namespace-test-request-reconstructed")

if jq('.var."namespace-test-request-original" != .var."namespace-test-request-reconstructed"') {
    error("service.NotTheSame", "Original service snapshot does not match reconstructed snapshot")
    exit
}

log("service detail snapshots are identical, all revisions where successfully reconstructed")