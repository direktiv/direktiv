http_proxy: ""
https_proxy: ""
no_proxy: ""

kong-internal:
  proxy:
    type: ClusterIP
  ingressController:
    ingressClass: kong-internal

kong-external:
  env:
    prefix: /kong_prefix/
    plugins: "grpc-gateway,grpc-stream"
