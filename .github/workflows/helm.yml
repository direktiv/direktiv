name: Build Helm
on:
  workflow_dispatch:
  create:
    tags:
      - v*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: build helm chart
      run: |
        version=`git describe --abbrev=0 --tags`
        echo $version
        echo "direktiv helm version $version"
