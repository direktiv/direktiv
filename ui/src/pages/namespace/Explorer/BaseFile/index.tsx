import React from "react";

const index = () => (
  <div>
    <h3>Openapi Base File</h3>
    <p>
      This file contains the OpenAPI specification for the gateway. It is used
      to define the API endpoints and services that the gateway will expose.
    </p>
  </div>
);

export default index;
