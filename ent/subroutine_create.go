// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/vorteil/direktiv/ent/subroutine"
)

// SubroutineCreate is the builder for creating a Subroutine entity.
type SubroutineCreate struct {
	config
	mutation *SubroutineMutation
	hooks    []Hook
}

// SetCallerID sets the "callerID" field.
func (sc *SubroutineCreate) SetCallerID(s string) *SubroutineCreate {
	sc.mutation.SetCallerID(s)
	return sc
}

// SetSemaphore sets the "semaphore" field.
func (sc *SubroutineCreate) SetSemaphore(i int) *SubroutineCreate {
	sc.mutation.SetSemaphore(i)
	return sc
}

// SetMemory sets the "memory" field.
func (sc *SubroutineCreate) SetMemory(s string) *SubroutineCreate {
	sc.mutation.SetMemory(s)
	return sc
}

// SetSubroutineIDs sets the "subroutineIDs" field.
func (sc *SubroutineCreate) SetSubroutineIDs(s []string) *SubroutineCreate {
	sc.mutation.SetSubroutineIDs(s)
	return sc
}

// SetSubroutineResponses sets the "subroutineResponses" field.
func (sc *SubroutineCreate) SetSubroutineResponses(s []string) *SubroutineCreate {
	sc.mutation.SetSubroutineResponses(s)
	return sc
}

// Mutation returns the SubroutineMutation object of the builder.
func (sc *SubroutineCreate) Mutation() *SubroutineMutation {
	return sc.mutation
}

// Save creates the Subroutine in the database.
func (sc *SubroutineCreate) Save(ctx context.Context) (*Subroutine, error) {
	var (
		err  error
		node *Subroutine
	)
	if len(sc.hooks) == 0 {
		if err = sc.check(); err != nil {
			return nil, err
		}
		node, err = sc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SubroutineMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = sc.check(); err != nil {
				return nil, err
			}
			sc.mutation = mutation
			node, err = sc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(sc.hooks) - 1; i >= 0; i-- {
			mut = sc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (sc *SubroutineCreate) SaveX(ctx context.Context) *Subroutine {
	v, err := sc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (sc *SubroutineCreate) check() error {
	if _, ok := sc.mutation.CallerID(); !ok {
		return &ValidationError{Name: "callerID", err: errors.New("ent: missing required field \"callerID\"")}
	}
	if _, ok := sc.mutation.Semaphore(); !ok {
		return &ValidationError{Name: "semaphore", err: errors.New("ent: missing required field \"semaphore\"")}
	}
	if _, ok := sc.mutation.Memory(); !ok {
		return &ValidationError{Name: "memory", err: errors.New("ent: missing required field \"memory\"")}
	}
	if _, ok := sc.mutation.SubroutineIDs(); !ok {
		return &ValidationError{Name: "subroutineIDs", err: errors.New("ent: missing required field \"subroutineIDs\"")}
	}
	return nil
}

func (sc *SubroutineCreate) sqlSave(ctx context.Context) (*Subroutine, error) {
	_node, _spec := sc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (sc *SubroutineCreate) createSpec() (*Subroutine, *sqlgraph.CreateSpec) {
	var (
		_node = &Subroutine{config: sc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: subroutine.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: subroutine.FieldID,
			},
		}
	)
	if value, ok := sc.mutation.CallerID(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: subroutine.FieldCallerID,
		})
		_node.CallerID = value
	}
	if value, ok := sc.mutation.Semaphore(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: subroutine.FieldSemaphore,
		})
		_node.Semaphore = value
	}
	if value, ok := sc.mutation.Memory(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: subroutine.FieldMemory,
		})
		_node.Memory = value
	}
	if value, ok := sc.mutation.SubroutineIDs(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeJSON,
			Value:  value,
			Column: subroutine.FieldSubroutineIDs,
		})
		_node.SubroutineIDs = value
	}
	if value, ok := sc.mutation.SubroutineResponses(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeJSON,
			Value:  value,
			Column: subroutine.FieldSubroutineResponses,
		})
		_node.SubroutineResponses = value
	}
	return _node, _spec
}

// SubroutineCreateBulk is the builder for creating many Subroutine entities in bulk.
type SubroutineCreateBulk struct {
	config
	builders []*SubroutineCreate
}

// Save creates the Subroutine entities in the database.
func (scb *SubroutineCreateBulk) Save(ctx context.Context) ([]*Subroutine, error) {
	specs := make([]*sqlgraph.CreateSpec, len(scb.builders))
	nodes := make([]*Subroutine, len(scb.builders))
	mutators := make([]Mutator, len(scb.builders))
	for i := range scb.builders {
		func(i int, root context.Context) {
			builder := scb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SubroutineMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, scb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, scb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, scb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (scb *SubroutineCreateBulk) SaveX(ctx context.Context) []*Subroutine {
	v, err := scb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
