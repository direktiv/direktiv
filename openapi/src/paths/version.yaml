
/api/v2/version:
  get:
    summary: Get server version information.
    tags:
    - server
    responses:
      '200': 
        description: Server version information.
        content:
          application/json:
            example:
              data: "1.0.0"
            schema:
              type: object
              properties:
                data: 
                  type: string
